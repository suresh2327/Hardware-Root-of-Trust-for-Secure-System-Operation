# 02_Architecture

This directory contains the architectural diagrams, block diagrams, and system-level design documents for the Hardware Root-of-Trust (RoT) module.

The architecture details how the secure boot sequence is enforced, the interaction between the SHA-256 cryptographic engine, and the firmware authentication state machine before CPU startup.
